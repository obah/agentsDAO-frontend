type Props = {};

export default function Carousel(props: Props) {
  return (
    <>
      <div>slideshow</div>
    </>
  );
}

/**
 * 2 sections - one for main image preview and two for images thumbnail
 * a function to handle image click in section 2 and update section 1
 * make images in section 2 a button?
 */
